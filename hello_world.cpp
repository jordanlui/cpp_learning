#include <iostream>

// Working C++ coding and compile in Sublime 3 on Windows
// Installed g++ and gcc with cygwin and added to windows path variables
// Now the g++ code copiles and runs
// Note that old conventions like iostream.h and cout instead of std::cout seem to not run
// The program is picky!

main()
{
    std::cout << "Hello World!";
    return 0;
}